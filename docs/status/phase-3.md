# Phase 3 — Feature store, serving, auth, multi-tenancy, synthetic-load harness (in progress)

> Moved out of `CLAUDE.md` on 2026-08-30 so the instruction file stays short; this folder is the full ledger and `CLAUDE.md`'s "Current status" is its summary. Update both when something lands: the ledger with the detail, the summary only when the shape of the project changes.

## 2026-09-03 closeout work

- ✅ **Ranker-training candidates now apply serving-shaped exclusions.**
  `_build_ranker_training_set` still requests unfiltered item-item candidates
  so the sampled target can remain the LambdaRank positive. It independently
  computes the user's history strictly before that target timestamp and removes
  those titles from the negative pool. Equal-timestamp interactions remain
  invisible, matching the feature pipeline's point-in-time rule. This closes
  remaining platform item (b).
- 🔲 **Item (a), the training feature source, is deferred — not closed.** What
  landed is the test, not the decision: feature-parity CI now proves all eight
  values agree across Python, Feast historical, and Feast online at a
  materialization timestamp, and the trainer records both source versions. A
  2026-09-03 ADR 0009 amendment declared the boundary permanent; it was withdrawn
  on 2026-09-04 because it never priced the alternatives. The owner deferred the
  decision the same day to prioritise the modeling ladder, which does not touch
  the materialization path. It becomes blocking before any full-25M
  materialization and before M2 opens the serving path. Costed options and the
  measured numbers: `docs/model-planning/memos/feature-source-boundary.md`
  (D-009).
- ✅ **Measured 2026-09-05: the exclusion's effect on the candidate mix is small,
  and its effect on ranking metrics is inside seed noise.** Two full-data runs,
  identical but for `RANKER_APPLY_SERVING_EXCLUSIONS`, on the same 154,003
  sampled positives. Both build **87,787 groups** — the same positives survive —
  and the training set differs by **356 rows out of 1.84M (0.019%)**, which is
  the handful of groups whose negative pool fell below 20 after exclusion.
  End-to-end at K=10: warm recall −1.61%, warm NDCG −0.14%, cold recall +0.75%,
  cold NDCG +0.54%, overall recall −0.86%, overall NDCG +0.39% (exclusions on
  relative to off). Every one of those sits at or inside the ranker's own
  measured seed spread at these settings — warm NDCG@10 relative range 1.68%,
  cold 2.10% — so **at one run per arm the difference is not distinguishable
  from noise**, and the honest claim is "no measurable harm", not "improvement".
  Two caveats kept rather than dropped: row counts understate the change,
  because the exclusion also alters *which* negatives are drawn in groups whose
  pool stayed at or above 20, and that composition shift was not counted
  directly; and under the 2026-09-05 one-run policy neither arm has a seed
  spread of its own, so the comparison inherits the limitation it illustrates.
- 🔲 **Dismissals have no offline counterpart.** Serving excludes watched *and*
  dismissed titles; MovieLens carries no dismissal events, so training applies
  the watched half and records the gap rather than papering over it. The named
  values live in `docs/model-planning/contracts/evaluation-protocol.md`.

Serving, auth, multi-tenancy, feature store, and the synthetic-load harness. The platform decisions are pinned by ADRs 0007–0011 (PRs #27–#31) and the serving platform landed in PRs #32–#44; ADR 0012 and frontend ADR 0002 (PR #45) pin the browser-identity and movie-discovery contracts, and the product itself landed as Bundles 1–7 in PRs #47–#65. The vertical-slice milestone and its definition of done are tracked in `docs/records/demo-plan.md`; the frontend redesign bundles in `docs/frontend/implementation-plan.md`, with the written gate in `docs/frontend/finish-gate-review.md`. Bullets are ordered by the PR that landed them. Status:

- ✅ **ADR 0007 + auth foundation** (PRs #27, #32, #33) — Keycloak self-hosted, realm per tenant, with `default` and `demo` realms seeded from `infra/keycloak/realms/`. `src/auth/middleware.py` validates tokens against a TTL-cached JWKS (`src/auth/jwks.py`), derives the tenant from the token issuer, and attaches a `RequestPrincipal` to `request.state`. A dev-only bypass is refused by `Settings.__init__` outside `environment == "dev"`.
- ✅ **ADR 0008 + tenancy foundation** (PRs #28, #32, #33) — Alembic migrations 0001–0009 under `alembic/versions/` create the `app_user` / `admin_user` roles, the `public.tenants` registry, `tenant_id` on every scoped table, and forced RLS policies. The middleware opens a per-request transaction with `SET LOCAL app.tenant_id`; pgBouncer (`infra/pgbouncer/`) runs in transaction-pool mode so the setting can't leak. `src/serving/tenancy/router.py` resolves a tenant to its config and Redis key prefix; `src/serving/startup_checks.py` refuses to boot if the app engine can bypass RLS or pgBouncer is in session mode. `tests/tenant_isolation/` runs in CI against the real compose stack.
- ✅ **ADRs 0009–0011** (PRs #29–#31) — Feast (Postgres offline + Redis online), k6 with Prometheus remote-write, and the fixed-seed cold-start cohort methodology are pinned ahead of their implementation bundles.
- ✅ **FastAPI serving skeleton** (PR #33) — `src/serving/app.py` with `/healthz` and authenticated `/whoami`, startup safety checks, and tenant-scoped database transactions.
- ✅ **Online recommendation path** (PRs #34, #43, #45) — `src/serving/recommendations.py` + `src/serving/orchestration.py`: authenticated warm requests retrieve item-item candidates, batch-read Feast/Redis features, rank with LightGBM, and filter live RLS-scoped history; cold or unavailable model paths fall back explicitly to popularity. Routing uses unique watched movie IDs against the shared `COLD_START_THRESHOLD` — histories below it are fallback, histories at or above it may take the learned path. (The constant was 5 when this landed; it is 10 since the 2026-08-30 amendment, and the offline models route on it too.)
- ✅ **Phase 3 baseline frontend** (PR #35) — Next.js user selector, recommendation grid, watch-history panel, serving-policy metadata, and a server-side FastAPI proxy. Frontend lint, strict type checking, and production build run in CI. Superseded by the movie-discovery product; retained only at `/legacy` (PR #65).
- ✅ **Durable demo personas** (PR #37) — `synthetic/personas/`: four named, tenant-scoped synthetic users (Action Fan, Drama Fan, Eclectic Viewer, and Cold Start), checked-in catalog/history fixtures, an idempotent `make demo-seed` path, authenticated persona discovery (`/personas`), and RLS isolation coverage. Migration 0005 tags them `synthetic=true`.
- ✅ **TMDB metadata and posters** (PR #38) — `src/serving/tmdb.py` resolves MovieLens `tmdbId` values server-side through a bounded TTL/LRU cache, degrades to MovieLens metadata when the token or upstream is unavailable, and the web app renders optimized posters with accessible visual fallbacks and required attribution. The token never leaves the backend.
- ✅ **Repeatable demo environment** (PRs #39, #40) — `docker-compose.demo.yml` layered on `docker-compose.yml` builds the FastAPI (`infra/api/`) and standalone Next.js images, bootstraps base tables plus Alembic migrations (`src/data/demo_setup.py`), loads a self-contained reviewed catalog/persona fixture, verifies readiness and warm/cold behavior (`synthetic/smoke/demo.py`), and supports isolated down/reset/log operations via `make demo-*`. Runbook in `docs/demo-runbook.md`.
- ✅ **Interactive rating loop** (PR #41) — `PUT /users/{id}/ratings/{movie_id}` and `DELETE /users/{id}/ratings` write through the RLS-bound request connection (migration 0006); history and recommendations are re-requested after the write commits. The star value is stored for display — the deployed learned path consumes the watched movie ID, not the rating magnitude (ADR 0012).
- ✅ **Feast/Redis feature path** (PR #42) — `src/features/feast_repo/` declares `tenant` / `user` / `item` entities and three feature views over `feature_store.*` Postgres tables; `src/features/materialize.py` publishes tenant-keyed snapshots into Redis; `src/features/online.py` exposes no unscoped read. A dedicated feature-server container (`infra/features/`) serves online reads. `tests/feature_parity/` runs in CI against live Postgres + Redis (non-negotiable #2).
- ✅ **Versioned learned serving** (PR #43) — `src/models/artifacts.py` defines a SHA-256-pinned `ServingManifest` binding the item-item index, the LightGBM booster, the tenant, and the ordered feature contract (`src/feature_contract.py`). `src/training/demo_artifacts.py` trains and publishes the bundle; `src/serving/model_server.py` is a private, token-protected sidecar that loads it once at startup so the slim API image never imports Feast, pandas, or LightGBM. `tests/learned_serving/` covers the manifest-backed path.
- ✅ **Prediction audit + k6 gate** (PR #44) — `src/serving/audit.py` + migrations 0008/0009 persist every recommendation's exact predictions, feature values, model versions, fallback reason, and per-stage timings into forced-RLS `recommendation_audits`, readable via `GET /users/{id}/audits` (non-negotiable #8). `synthetic/load/` k6 scripts (version pinned in `infra/ci/k6-version`) drive real-Keycloak warm/cold/mixed traffic against a bypass-disabled `api-load` service and gate p99 < 100 ms, zero errors, correct responses, and >50 requests/second; the `synthetic-load-smoke` CI job runs the 60-second profile (non-negotiables #4, #11). Accepted 2026-08-20 baseline: p50 6.31 ms, p95 14.27 ms, p99 41.30 ms. Hardened 2026-08-21 (PR #59; ADR 0010, "measuring the service rather than the runner") so the gate measures the service and not CI contention — the unmeasured services are quiesced, `setup()` primes every worker × persona, the VU ceiling no longer drops slow arrivals, and warm traffic must prove `serving_policy.learned` rather than merely returning 200. The same note covers the second round: promotion-only CFS priority for the measured services (nothing is demoted except Prometheus, so the sibling browser-auth job is unaffected), no Prometheus in the 60-second profile, per-second latency buckets joined against host CPU steal, an uploaded evidence artifact, and a documented re-measure-once rule for windows breached under hypervisor preemption. No threshold, arrival rate, or workload changed in either round.
- ✅ **Movie-discovery product contract** (PR #45) — `docs/frontend/` (product discovery, route-level design contracts, backend-readiness matrix, implementation plan, testing strategy and finish gate, baseline evidence) and frontend ADR 0002 define a poster-first Discover / Browse / Library / detail experience with progressive disclosure of ML evidence. Cross-cutting ADR 0012 pins browser identity (actor vs. persona, `/me/...` resources, PKCE via a Next.js BFF session), the `user_movie_state` + `user_feedback_events` read model, feedback-transition semantics, commit-before-acknowledge durability, and what a rating does and does not change online.
- ✅ **Bundle 1 correctness and contract foundation** (PR #45) — the auth middleware commits the request transaction *before* returning a successful response (no 2xx for a mutation or fail-closed audit that can still fail to become durable); tokens must carry `aud=movielens-api` and an `azp` in the explicit allow-list (`movielens-api`, `movielens-web`); arbitrary persona selection requires the confidential service client or the `demo-impersonator` realm role; a verified realm with no `public.tenants` row is rejected at the authenticated boundary. `scripts/generate_openapi.py` commits `docs/api/openapi.json`, `web/lib/api.generated.ts` is generated from it, and both drift checks run in CI (`make api-contract-check`, `make web-api-types-check`).
- ✅ **Bundle 1 browser session** (PR #47) — Auth.js runs the real Keycloak authorization-code + PKCE flow, keeps API tokens in an encrypted HttpOnly server session, rotates access tokens, propagates logout, enforces Origin/CSRF on BFF mutations, pins the public issuer separately from internal Compose routing, and passes bypass-disabled Playwright against a freshly imported realm. The pre-redesign responsive/error/fallback screenshot matrix is committed for finish-gate comparison.
- ✅ **Bundle 2 durable feedback and Library foundation** (PR #48) — migration 0010 creates forced-RLS current movie state (`user_movie_state`) and append-only actor-attributed feedback events without rewriting imported ratings. Idempotent revisioned watched/rating/watchlist/dismissal mutations, cursor Library APIs, truthful `live-ratings-v1` taste summaries, and the authenticated selected-persona `/library` route consume the server-owned Auth.js session boundary.
- ✅ **Bundle 3 scalable catalog and movie detail** (PR #50) — migration 0011 persists a local movie-metadata read model so a poster-rich grid never fans out to TMDB per card. `GET /users/{id}/catalog` serves deterministic search, genre and year filters, three sorts, and opaque cursors bound to the normalized query fingerprint (a cursor reused against a different query is a 400, and no total is invented); `GET /users/{id}/movies/{movieId}` serves detail. Both overlay the durable Bundle 2 movie state, and the reviewed demo fixture grew to 120 titles with 24 enriched posters. Contract in `docs/frontend/catalog-contract.md`.
- ✅ **Bundle 4 movie-discovery frontend system** (PR #52) — semantic visual and motion tokens, responsive desktop/mobile navigation, and the reusable poster, rail, collection, state, rating, drawer, loading, empty, and error primitives every product route is built from, with authenticated preview shells at `/ui-preview` and typed fixtures that fail closed in production. Vitest, RTL, jest-axe, and a responsive Playwright matrix at 390/768/1440 arrive with it. Documented in `docs/frontend/frontend-system.md`.
- ✅ **Bundle 5A live resource boundary** (PR #53) — `web/lib/resources/` is the one server-owned client that reaches FastAPI: per-resource timeout budgets, narrow hand-written validators over the generated OpenAPI types, `X-Request-ID` generation/adoption/echo, `private, no-store` on personalized BFF responses, caller-supplied bearer tokens refused at the BFF edge *and* in the browser reader, and a fixture lockout asserted structurally by reading the module's own source. Its `loading`/`retry`/`ready`/`empty`/`forbidden`/`auth-expired`/`not-found`/`upstream-error` state model renders through one region component, so a failed resource never blanks the regions around it.
- ✅ **Bundle 6 backend — positive history separated from exclusions** (PR #54) — `src/serving/recommendations.py` reads positives (watched, not dismissed) and exclusions (dismissals plus already-seen) from `user_movie_state` in one round trip; `src/serving/orchestration.py` passes them to the sidecar as distinct inputs and re-applies the exclusion set at the popularity fallback, candidate retrieval (`src/models/artifacts.py`), metadata hydration, and a final fail-closed check. A dismissed title never seeds item-item retrieval and is never written back as a rating or training negative (ADR 0012). `src/serving/policy.py` pins the shared filter-policy, score-scale, and input-digest vocabulary; the recommendation response gains an additive `serving_policy` object (name, learned, positive-signal count, threshold, structured reason, score scale, filter policy) and migration 0012 adds the input-state revision/hash, exclusion hash, feature event time, filter policy, candidate-source contributions, and structured reason to `recommendation_audits`, all readable via `GET /users/{id}/audits`.
- ✅ **Request correlation across the whole surface** (PR #54) — `src/serving/request_id.py` adopts a well-formed inbound `X-Request-ID` (1–128 printable ASCII characters, no whitespace) and echoes it on every response, minting a UUID otherwise; `recommendation_audits.correlation_id` stores the echoed value while `request_id` stays the row's own identity so a replayed header cannot collide. Tenant-isolation canaries now also cover `/users/{id}/features`, `/users/{id}/catalog`, and `DELETE /users/{id}/ratings`.
- ✅ **Bundle 5B–5D — the movie-discovery route slices** (PRs #55, #56, #57 — 5D, 5B, and 5C in the order they merged) — every route reads through the 5A boundary and claims only what the response reports. `/discover` loads recommendations and watch history as independent server-owned regions, with the technical evidence (`Why this?` → prediction audit + online features) reachable in two deliberate actions so it can never delay the first movie; the policy label follows the reported `serving_policy.learned` flag rather than an inference, and the rank score appears only inside the disclosure, labelled as an uncalibrated ordering. `/browse` and `/movies/[movieId]` sit on the Bundle 3 catalog contract with URL-owned filters, de-duplicated cursor pages over the endpoint's total ordering, a stale cursor restarted from the top behind a plain notice, and per-tab restoration of the loaded window and scroll position after a detail visit. `/library` reads Rated, Watchlist, and History independently per tab with URL-owned state, optimistic reconciliation against the committed revision, and `Remove rating` kept quiet and distinct from a confirmed `Remove from history`.
- ✅ **Bundle 6 Quick Picks** (PR #58) — `/quick-picks` is one decision at a time on the exclusion-aware serving contract. A pure reducer owns the queue, so buttons, `J`/`K`/`L`/`U`, and pointer swipes dispatch the same action — parity is the mechanism, not a convention — and the card advances only after the API commits, because a one-card queue that advanced optimistically and rolled back would re-show a title the viewer already dismissed. Progress toward the cold-start threshold is read from `serving_policy.positive_signal_count` and learned copy waits for a returned `learned === true`. `web/lib/quick-picks/contract.ts` is one assertable statement of what each action means: watchlist is organizational, watched is one positive interaction, dismissal is an undoable exclusion and never a training negative (ADR 0012).
- ✅ **Serialized service-backed browser journeys** (PR #60) — `web/playwright.config.ts` pins `workers: 1` for the specs that run against the seeded Compose stack, and each journey owns one persona and reverses its own writes (Action Fan for movie state and Library, Drama Fan for Discover, Eclectic Viewer for Browse, Cold Start for the PKCE reset and then Quick Picks, which needs the zero-signal state the reset leaves behind). Three journeys had been writing the same persona's revision, producing 409s and cross-test flakes; serialized, the whole set runs faster than the contended parallel one. Sign-in moved to a shared `web/tests/e2e/keycloak.ts` helper. The fixture-mode harness (`playwright.ui.config.ts`) is untouched and still fully parallel.
- ✅ **Bundle 7c — one control family, one write path** (PR #61) — `web/components/movie/movie-state-controls.tsx` replaces the three per-slice control sets with a declared, ordered control set per surface, and `web/lib/movie-state/` becomes the only way any surface commits: the ADR 0012 transition table written once, the intent-bound idempotency key, `expected_revision`, the conflict re-read, rollback with an announced restore, the focus walk, and the committed-state relay. The copies had already diverged — one optimistic table left a watchlist entry standing after a movie was marked watched, and only one of the two mutation clients turned a `409` into a correction rather than telling the viewer to reload. Behaviour-preserving; `docs/frontend/frontend-system.md` is the resulting system of record.
- ✅ **Bundle 7b page-shaped load budgets and browser timing** (PR #62) — `synthetic/load/pages.js` models each route's real fan-out, cursor continuation, Library read, mutation-plus-immediate-read, and Quick Picks action sequence as tagged per-step k6 workloads with measured p95/p99 budgets in a separate thresholds module (the pinned recommendation gate is untouched); `web/tests/perf/` measures LCP, CLS, and time-to-visible-acknowledgement separately in a real browser on the pinned mobile profile and asserts the structural layout promises; `synthetic/load/reliability.py` proves request-id traceability into the audit row, the auth boundary, dependency provenance, bounded pages, cursor rejection, and degraded-metadata operation, and records that rate limiting is not implemented. Both suites follow the persona ownership table the browser journeys keep in `web/tests/e2e/browser-auth.spec.ts`, undo every write, and refuse to run — or to finish — if anything touched the cold-start persona. Page correctness, browser CLS, LCP, and the structural claims are enforced in CI; the per-step latency budgets and the acknowledgement budget are advisory until runner data exists, with the promotion rule written into ADR 0010's 2026-08-21 page-shaped note.
- ✅ **Bundle 7a frontend finish gate — HOLD** (PR #63, `docs/frontend/finish-gate-review.md`) — the handoff's ten-step journey now runs end to end against the seeded Compose stack with `DEV_AUTH_BYPASS=false` (`web/tests/e2e/finish-gate-journey.spec.ts`, in the serialized `browser-auth-e2e` set) and the visual/accessibility gate covers the named state matrix at 390/768/1440 plus a 320px sweep (`web/e2e/finish-gate.spec.ts`, in the `frontend` job); the written review records **HOLD** on three cutover items — `/` still serves the pre-redesign dashboard and its static `Candidate policy: Popularity baseline` panel contradicts the `item-item-cosine+lightgbm` policy the same session's API reports, `/discover` has no inbound link from any surface, and Browse and movie detail run a second shell without the contracted mobile bottom navigation — while every criterion applied to the five product routes themselves passes.
- ✅ **Item-item retrieval seeds from watched history again** (PR #64) — the exclusion set the coordinator sends the sidecar necessarily contains the user's own watched titles, and `CandidateIndex.retrieve` was using it to filter the seed set, so every warm persona was in fact served the index's popularity fill scored by LightGBM while the response reported `learned: true` over "0 positive seeds" (finish-gate finding N2); dismissals now travel on their own `dismissed_movie_ids` input as the only signal that may drop a seed, `seed_count` reports the seeds retrieval actually used rather than the ones offered, and a retrieval no seed reached is reported as `unseeded-retrieval` / `popularity-fill+lightgbm` with `learned: false` instead of borrowing the learned label.
- ✅ **Bundle 7d cutover — the product is the front door** (PR #65) — `/` serves the movie-discovery product to a signed-in viewer and the sign-in door to everyone else; the pre-redesign dashboard lives only at `/legacy`, labelled as the retained rollback, and its serving-contract panel reports the `serving_policy` the response carried instead of the `Popularity baseline` constant that contradicted it; every primary navigation points at `/discover`; and Browse, movie detail, and Library render the one shared `AppShell`, so the two parallel headers are deleted and the contracted mobile navigation and resolved persona name belong to the product rather than to three routes out of five. The re-run gate (`docs/frontend/records/finish-gate-passes.md` §10) — run twice, once on the branch's own base and once against a tree carrying PR #64, since that landed mid-review and changed which titles every persona is served — records **all seven criteria passing and the three blocking items cleared** — the verdict stays **HOLD only because moderated sessions with real participants (4–5 viewers, 3–4 technical reviewers) are still owed**, which is not something a reviewer can substitute for. Putting `/` into the accessibility gate for the first time found three defects on the signed-out door — a 1.31:1 primary-action label, a 2.42:1 note, and a 320px overflow — all fixed here; the label was structural, since `button { color: inherit }` is unlayered and out-ranks every Tailwind utility.
- ✅ **Model-server native parallelism pinned, and the load gate instrumented** (PR #69, merged at `c2db933`) — each of the four model-server uvicorn workers was letting LightGBM/OpenMP and BLAS size a native thread team to the whole host, so process parallelism multiplied by native parallelism into a periodic CPU backlog: p50 81.57 ms / p95 551.37 ms / p99 903.64 ms at 0% host steal. With only `OMP_NUM_THREADS`, `OPENBLAS_NUM_THREADS`, `MKL_NUM_THREADS` and `VECLIB_MAXIMUM_THREADS` set to `1`, the same unchanged gate passed at p50 7.24 ms / p95 12.50 ms / p99 48.99 ms. No threshold, timeout, worker count or durability rule moved. ADR 0010 carries the evidence, and the gate now also records the server-side handler latency next to k6's, per-second `srv_p99`, WAL/IO counter deltas and an `fdatasync` baseline on the Postgres volume — the instrumentation that distinguishes a slow service from a slow runner, which is what the two red CI runs on this fix's own branch turned out to need.
- ✅ **Production deployment — one Hetzner VPS (ADR 0013, `docs/deployment-runbook.md`)** — the system has a deployment rather than a plan for one. **ADR 0013** pins a single Hetzner CX22 (2 vCPU x86-64, 4 GB, 40 GB, Ubuntu 24.04) running `docker-compose.prod.yml` unchanged behind its own Caddy edge with `EDGE_TLS=acme`, with **exactly two public hostnames** — `PUBLIC_APP_HOST` and `PUBLIC_AUTH_HOST` — and every other service on the host's private Docker network, the only bound ports being 80/443 (the edge) and 22 (key-only SSH). Cost is the deciding factor and says so: ≈€4.50/month against a measured ≈$27 on Railway for the same ≈2.0 GB idle footprint, with the single-host consequences stated honestly (one failure domain, a deploy is a brief outage, patching is the owner's, backups off-box are the recovery story). The mechanics: CI's `publish-images` job pushes every image to GHCR as `ghcr.io/kudratsingh/movielens-recsys/<name>:<sha|main>` for `linux/amd64` only after every other job is green, `infra/host/bootstrap.sh` turns a stock Ubuntu box into this host idempotently (deploy user, sshd hardening, `ufw` 22/80/443, unattended upgrades, Docker CE, 20 MB × 5 log rotation, and the `movielens` / nightly-backup / weekly-prune systemd units), and `infra/deploy/deploy.sh <sha>` runs a release — pull at the SHA, the release jobs, `up -d`, `make prod-verify` — **rolling back to `.release/previous` automatically and re-verifying when verification fails**, with `DEPLOY-OK` / `ROLLBACK-OK` as its sentinels. `.github/workflows/deploy-production.yml` fires on CI's successful run on `main`, re-asserts the SHA's CI conclusion, and drives that over SSH with a `known_hosts` pinned from a secret (never `StrictHostKeyChecking=no`); `production-canary.yml` runs `make prod-verify` on the box every thirty minutes and leaves the advisory `make prod-load` to an explicit dispatch. The production-mode rehearsal was the first boot of this codebase with `ENVIRONMENT != dev`, and it earned its keep: a `Settings` failure respawning forever inside uvicorn while the container reported healthy, an API started by a line no deployment uses, pgBouncer's `auth_query` mode failing server-side SCRAM through the forced-user aliases (`userlist` is now the default, measured rather than preferred), Keycloak provisioning that had never once completed, and four smaller defects — all fixed, with the pinned k6 gate passing at the production topology at p50 6.85 ms / p95 9.47 ms / p99 12.93 ms. The sub-decisions ride in the same ADR: the serving bundle and Feast registry stay **baked into the sidecar image**, so rolling back the model is rolling back the image; `/readyz` remains a second unauthenticated path (no tenant data, no user data, unpublished port) and the widening of non-negotiable #10's wording stays recorded rather than quietly done; `/metrics` is deliberately **not** added; Postgres traffic stays on the host's private Docker network; GHCR is the image source of truth, so no retention policy may expire SHA tags; and production is **additive-migrations-only**, paired with a release step that exits 0 when the database is ahead of the image so a rollback's schema step cannot fail on "Can't locate revision". **`synthetic/load/thresholds.js` in CI remains the SLO's only authority** — the production canary records p99 with no verdict, doubly so now that k6 runs on the same two vCPUs as the service it measures. **ADR 0014** closes product-track item (c) with the per-`(tenant, subject)` token bucket in `src/serving/ratelimit.py`, on by default outside `dev` — and carries the rehearsal's correction: the bucket is per worker and keep-alive pins a client to one of them, so at the first defaults (120/minute, burst 30) **37.9% of one subject's 301 canary requests were refused**; the defaults are now 600/minute with a burst of 120 and a Redis-backed shared bucket is the named follow-up.
- ✅ **Product round on the cutover build (PRs #72, #77, #78, #79, #81, and the Seen PR)** — a live-browser sweep found the first press of any Discover or Quick Picks control being silently discarded (a revision-0 assumption; the one write client now completes the intent once at the canonical revision with the same idempotency key) and that 96 of the 120 fixture titles had no poster by construction (a committed, idempotent TMDB enrichment fills all 120, liveness-checked by `make catalog-verify`). The featured slot became a queue position that advances on Watched / Watchlist / Not-for-me; the rating prompt ends itself and hands the page back to the featured movie; the ranked rail got one control baseline, no wrapping labels, and a real elevation scale; the movie page gained TMDB details — tagline, runtime, backdrop, cast, a click-to-load privacy-enhanced trailer — over a `details` JSONB read model (migration 0013) and the shared `RatingStars`; a watchlisted title in the featured slot can be skipped and, after three skips, a persisted per-persona preference (migration 0014, forced RLS, `GET`/`PUT /users/{id}/preferences`) stops featuring them; and the Library's History tab became **Seen** — search, genre and year filters, five rankings with fingerprint-bound cursors and an exact `matched` count, and a spotlight that walks the same filtered list with a quick re-rate and the confirmed removal (`docs/frontend/seen-contract.md`). ADR 0012's semantics are unchanged throughout. Two measurement fixes rode alongside: CI's Postgres runs on tmpfs for the k6 gate so the runner's disk is not in the measured path (PR #80 — the same rail branch went from a p99 of 294 ms on disk to 16 ms), and the unit suite pins OpenMP to one thread so LightGBM's and scikit-learn's two `libomp` copies cannot crash a macOS test process (PR #82).
- ✅ **ADR 0011 closed — the cold-start cohort, and what it measured** (`synthetic/cold_start/`, migration 0015, the ADR's 2026-08-29 addendum) — cold-start routing was the one non-negotiable (#3) still resting on an assertion in ADR text rather than on a number. It now has one. A fixed-seed 2 000-user cohort — 500 users at each history size {0, 1, 3, 10}, items drawn popularity-weighted from the train catalog, one held-out target each — is generated into a DVC-tracked parquet, attached to the training frame before `fit`, and scored per bucket through `EvalResult.synthetic_cold_slices`, so every trainer logs `synth_cold_recall_*_h{0,1,3,10}`, `synth_cold_fallback_served_h*` and a `synth_cold_routing_ok` tag. The target is the *first* draw of the weighted sample-without-replacement rather than the last, because only the first has the marginal of a single popularity-weighted draw whatever follows it — taking the last would have made bucket 10's target systematically less popular than bucket 0's and confounded the comparison the buckets exist for. Determinism is built on `Generator.random` over a PCG64 alone, never `Generator.choice`, whose selection algorithm is an implementation detail; against MovieLens 25M the generation is byte-identical on a re-run (9 000 rows, 2 000 users, 1 313 distinct targets, fingerprint `ae4475f0…`), and the loader refuses a cohort whose recorded MovieLens md5 does not match the checkout rather than reporting recall for items the model may never have seen. The 7 000 attached rows are 0.035% of train and no synthetic user appears in holdout, so every warm/cold and per-policy number already in MLflow is unmoved. **The first thing it measured was a divergence:** the four offline candidate models' `was_served_by_*` predicates all reduce to "is this user in the fitted index at all?", so a 1- or 3-interaction user takes the learned path offline while ADR 0001 and the deployed serving path both send them to the popularity fallback — fallback counts of 500/0/0/0 where the threshold implies 500/500/500/0, and `synth_cold_routing_ok = false` on every candidate run. Item-item on the full 25M dataset reads h0 0.4760 / h1 0.1440 / h3 0.2880 / h10 0.3900 recall@500, and the h0-vs-h1 pair is the clean comparison — identical target distribution, differing only in history size and routing — so a 1-interaction user served one film's cosine neighbours does about three times worse than the fallback the threshold says should have taken them. Evidence for a direction, not a verdict: h0 is flattered by the popularity-weighted targets ADR 0011's Risks section warns about. That is reported rather than repaired here: the expectation is derived from `COLD_START_THRESHOLD` instead of from what a model happens to do, precisely so the gap stays a number, and closing it moves every existing warm/cold metric and is its own unit (platform track, item (f)). The `synth_cold` tenant is registered by migration 0015 with **no Keycloak realm** — these users never authenticate and the realm-drift job would carry an idle realm forever — and the Prefect regeneration task the ADR names belongs to Phase 4; both deviations are recorded in the ADR rather than done quietly.
- ✅ **Public face brought up to date, and the follow-ups it was carrying (PRs #86–#98, 2026-08-29)** — three audits of the public surface (README, every file under `docs/`, and the architecture as the code actually has it) found the docs one to six PRs behind the tree: the README described production in the present tense while nothing is deployed, no screenshot was embedded anywhere despite 184 committed, no offline metric value existed in the repository, rate limiting was called "not implemented" in five documents after ADR 0014 shipped it, `docs/demo-plan.md` still walked the pre-cutover dashboard, and the `/legacy` rollback diff no longer applied. What landed, in merge order: the deploy workflow stays a green no-op until a production host is configured, the same guard the canary already had (#86); `.gitignore` covers the agent worktrees, the `create-next-app` scaffold SVGs are gone, and the repo gained a PR template, two issue forms, `SECURITY.md`, a label taxonomy applied to every open issue, and a 1280×640 social-preview asset (#87); every staleness finding fixed, the records moved under `docs/records/` and `docs/frontend/records/` with banners, the 1,175-line finish-gate review split into a live verdict and a verbatim record, `docs/api/overview.md` listing all 20 paths / 25 operations, `docs/README.md` as the reading path, an evidence index, and `synthetic/` + `infra/` READMEs (#89); the Rated tab ranks by release year and TMDB score and the LIKE escape lives once in `src/serving/sql.py` (#88); the trailer-close flake (#83) root-caused to `scroll-behavior: smooth` after focus moves to *Close trailer* at 1440 — the box was always reserved — and the test now spends the trailer's own costs before asserting the close (#91); ten Mermaid diagrams rendered to committed light/dark SVGs by `web/scripts/render-diagrams.mjs` under `make diagrams`, drawn from the verified facts rather than this file's aspirations (no `/metrics`, no request-time TMDB call, the feature-server off the ranking path, no champion routing), plus `docs/architecture.md` for an outside reviewer (#92); Discover's rating prompt adopted the shared `RatingStars` (#93); a reproducible current-build screenshot matrix at `docs/frontend/evidence/current/` with a committed capture script that records the live `serving_policy` per persona and refuses to shoot a warm persona off the learned path (#94); the README rewritten as a front door — honest status block, quickstart near the top, the request-path diagram, a measured-latency table with dated sources, ADRs 0013/0014 in the table, a license line (#95); finding N7 closed by deleting the three unlayered resets that were verbatim duplicates of Tailwind's preflight and layering the rest, which also repaired a never-noticed white-on-white Retry button on the movie-page error state (#96); the Seen tab's filter row, which the capture run found collapsing the search field to 34 px below 1024 px, now wraps and is gated at 320/390/768 (#97); and `docs/results.md` — the first committed offline numbers, every one from `src/evaluation/` on the full 25M rows: popularity warm recall@10 0.0163 / NDCG@10 0.0309, CF/ALS 0.0338 / 0.0578, item-item warm recall@500 0.4001 / NDCG@500 0.1392, LightGBM LambdaRank warm recall@10 0.0394 / NDCG@10 0.0554 — with three deterministic EDA figures rendered by `make eda` and embedded in `docs/eda.md` (#98). Two findings from the results run are recorded rather than smoothed over: the ranker lifts warm recall@10 by 16.5 % over CF/ALS but its warm NDCG@10 falls 4.2 % while the overall figure clears ADR 0001's +3 % gate on the cold slice alone, so which slice the Phase 4 gate reads needs settling before it is automated; and item-item's top-500 contains only 56.9 % of the ranker's sampled training positives. The two-tower did not finish a 90-minute CPU budget (one epoch took 38 minutes), so ADR 0004's item-item-vs-two-tower comparison on the full dataset stays open with 0.4001 as the number to beat.
- ✅ **Platform and product follow-ups, in one concurrent wave (PRs #104–#114, 2026-08-29/30)** — the no-attribution convention was withdrawn (#104) and the public framing corrected: the models are the main line of work, the harness is what Phase 3 finishes so a real user can use them (#109), with the model ladder — two-tower v2, SASRec, a sequence-aware ranker, multi-objective ranking, mixing and re-ranking, bandits, the generative/foundation end state — written down in `docs/modeling-roadmap.md` under an approval gate: every rung is an ADR proposal that starts only when its decision-log row says *approved*, and rungs can be skipped with a recorded reason (#114). TMDB attribution moved into the shared `AppShell` footer with in-browser contrast (6.38:1) and mobile-nav clearance asserted at 320/390/768/1440, so `/legacy` retirement is no longer blocked by it (#105, closes #73). The tenant-isolation positive control stopped being vacuous: a paired sentinel matrix proves the owner's strings are *present* and the other tenant's *absent* on the same read, an RLS-engagement test proves the row exists one layer down, and the suite passes on both an empty and a seeded database with the state named in the test id (#107, closes #75). A refused transition is a `422` with `code: transition_refused` and a stale revision a `409` with `code: revision_conflict`, carried on the exception classes and rendered by one handler; the web client branches on the code and its six message regexes are gone — the compatibility endpoints had been turning refusals into 500s (#111, closes #74; `web/.prettierrc` at 100 columns added but not enforced, since the tree is hand-wrapped tighter than Prettier joins — adopting it is a whole-tree reformat for its own commit). The environment split landed honestly: `make up-dev` is an alias for the demo stack, which already *is* the dev environment, and `docker-compose.staging.yml` is a 101-line overlay on the production file with CI asserting the staging model differs from production by exactly its eight `ENVIRONMENT` labels (#106, closes platform item (e)). `public.tenants` gained `champion_{candidate,ranker,feature}_version`, `rate_limit_{requests_per_minute,burst}`, `ab_bucketing_seed` and `updated_at` (migration 0016) — a CHECK forbids a half-named champion, the seed comes from a `BEFORE INSERT` trigger over an immutable id hash, the registry read reuses the request's own transaction so a cache miss under load cannot deadlock the pool, the sidecar refuses a bundle whose versions are not the tenant's champion with a coded 409, a tenant with no champion takes the fallback with the structured reason `no-champion`, and verify's V-12 now fails on a bundle serving under a version nobody promoted; one sidecar still serves one bundle, and routing is Phase 6 (#112, closes platform item (c)). The rate limiter became one bucket per `(tenant, subject)` across every worker — an atomic Lua token bucket on Redis's own clock, `EVALSHA` with a load fallback, fail-open to the in-process bucket with a rate-limited log line and a reported-only `/readyz` field, `memory` refused outside `dev` unless explicitly acknowledged, the per-tenant columns read through the middleware's one `policy_for` seam, and the reliability check tightened to bounds that distinguish one bucket from four; found on the way: redis-py's default retries cost 6.7 s on a refused connection before failing open, now 0.2 ms (#110, closes #70 and ADR 0014's named follow-up). Every authenticated endpoint now writes a forced-RLS `request_audits` row (migration 0017) inline on the request's own transaction — the written tradeoff in ADR 0012's 2026-08-29 note is that a read now pays its first `fdatasync` rather than a second, recommendations keep their richer table and are skipped rather than duplicated, a failing handler is audited out of band, and `GET /users/{id}/audits?kind=requests` reads them back (#108, closes platform item (d)). The nineteen Dependabot alerts that appeared when alerts were switched on — all npm, in `web/` — were cleared with `next` 16.2.12, `vitest` 3.2.7 and two overrides (`postcss` 8.5.26, `sharp` 0.35.4, each checked against Next's real call sites) rather than a forced minor bump (#113). Left deliberately open: the two-tower's full-dataset run and the cold-start routing memo are on `docs/offline-decisions-twotower-routing`.
- ✅ **The cold-start threshold is 10, online and offline** (ADR 0001's 2026-08-30 amendment, ADR 0011's note, `docs/cold-start-routing-decision.md`'s Decision line) — the owner took both halves of the routing question at once: *"the first ten should be popular, then personalized."* `src/evaluation/protocol.COLD_START_THRESHOLD` is **10**, and it is now the **offline** candidate models' routing rule as well as the deployed one (the memo's option (a)) — `cold_start_threshold=COLD_START_THRESHOLD` is the constructor default on `CFModel`, `ItemItemModel` and `TwoTowerModel`, `routing.DEFAULT_POLICY` is `threshold`, and `None` / `SYNTH_COLD_ROUTING=index` survives as the explicit opt-out with its runs renamed `<base>-index-routing`. The evidence the decision rests on is that the objection against it was measurable and false: on MovieLens's natural holdout exactly **one** of 2 641 users sits in the disputed band, so every warm figure is identical to the last digit and cold/overall move by at most 0.08%, while on the ADR 0011 cohort item-item's h1 recall@500 goes **0.144 → 0.460**. One number now answers "is this user cold?" everywhere, which is the feature-parity argument (non-negotiable #2) applied to routing instead of to features. Every consumer derives it rather than restating it — the coordinator's fallback and its reported `serving_policy.threshold`, the reason string `cold-start: N positive watched signals below threshold 10`, ADR 0011's `expected_fallback_served` (unchanged at 500/500/500/0, and h10 now sits *exactly* on the boundary, which is a sharper test of the thing that bucket exists for), `src/release/verify.py` V-3/V-5 and `synthetic/smoke/demo.py` (both read the response), the two k6 scripts (a named constant checked *against* the response, since k6 cannot import Python), and the frontend, which had one named constant and one stray `?? 5` — now a single `LEARNED_SERVING_SIGNAL_THRESHOLD`, used only where a response reports no threshold of its own. The demo personas moved with it: **Action Fan and Drama Fan carry 12 watched titles each** (was 8), a deliberate margin of two over the boundary so a journey that dismisses a title cannot tip them onto the fallback mid-run; Eclectic Viewer stays at 11, Cold Start at 0. Verified on the running stack rather than asserted — all three warm personas report `learned: true` at 12/12/11 signals with `threshold: 10`, and Cold Start reports `popularity` with `cold-start: 0 positive watched signals below threshold 10`. `docs/results.md` carries a dated banner: every number on it predates the change and was scored at threshold 5 under index-membership routing, so the memo's head-to-head tables — both arms run at the same threshold — remain the right comparison between *policies*, while a future unsuffixed run means the threshold policy.
- ✅ **The promotion gate reads overall NDCG@10 and refuses a slice regression, and its tolerance was measured (`src/evaluation/gate.py`, ADR 0001's 2026-08-30 amendment, `docs/results.md`'s promotion-gate section)** — ADR 0001 gated on "+3% relative NDCG@10 **on the holdout**" while also requiring warm and cold to be reported separately, and never said which of the three numbers the gate read; on the first full-dataset comparison that ambiguity decided the verdict (+10.57% overall, +15.39% cold, **−4.16% warm**, with 26.6% of the users carrying 78.6% of the aggregate's NDCG mass). The owner took `docs/promotion-gate-slice-decision.md`'s option (c): **overall NDCG@10 at +3%, and no slice may regress beyond a tolerance set from measured seed-to-seed variation.** `promotion_decision` is a pure typed function over two `EvalResult`s — the aggregate clause as a positive claim the challenger must establish, each slice clause as a negative claim that does not block when the incumbent slice is empty or zero, `EvalResult.k` equality asserted so a recall@500 result can never be compared with an NDCG@10 one, and a structured verdict naming which clause failed and by how much — with `python -m src.evaluation.gate --candidate <run> --incumbent <run>` / `make gate` reading two MLflow runs (exit 0 promote, 1 refuse, 2 undecided) as the shape Phase 4 will call. The tolerance was then **measured, not chosen**: `TRAIN_SEED` (`src/training/seeds.py`, default unchanged) re-ran CF/ALS and the ranker at seeds 42/7/13 on the full 25M rows, and a seed-42 re-run reproduced both 2026-08-29 runs of record on every logged metric (38 and 36, zero differences) so the spread is seed-to-seed and nothing else. Popularity and item-item are deterministic and were skipped, stated rather than omitted. Relative NDCG@10 range across three seeds: CF/ALS warm 3.12% / cold 0.04%; **ranker warm 28.68% / cold 3.17% / overall 5.81%** — so at 2× the largest range, rounded up, `T_warm` = **58%** and `T_cold` = **7%**. Two findings ride with it and are the reason this unit matters more than the verdict: the ranker's warm regression is far inside its own noise (the same comparison reads **−4.16%, −17.24% and +13.59%** at the three seeds — the sign is not determined), so the gate **would promote** today; and the ranker's *overall* spread of 5.81% is **wider than ADR 0001's own +3% threshold**, so a single seeded run cannot establish a +3% improvement at all. Seed-averaged over three runs the comparison reads warm −2.76%, cold +13.24%, overall +9.27%, which does clear it. The cause is `RANKER_POSITIVE_LIMIT = 20 000` — a re-seed is a different training set, not a different tie-break — and a larger sample, not more seeds, is the fix. Nothing was promoted; `pipelines/` is still empty.
- ✅ **The ranker trains on the whole trailing window, and the promotion gate's tolerance tightens to what that buys (`src/training/sampling.py`, `src/evaluation/aggregate.py`, ADR 0005's 2026-08-30 note, `docs/results.md`'s sample-size section)** — the gate's noise floor left one number too large to live with: the ranker's warm NDCG@10 moved **28.68%** of its own mean across seeds and its *overall* NDCG@10 **5.81%**, wider than ADR 0001's own +3% threshold, because `RANKER_POSITIVE_LIMIT = 20 000` meant a re-seed was a different training set. The measurement nobody had taken: **the 30-day trailing window positives are drawn from holds 154,003 rows**, so 20,000 was 13.0% of what was already eligible — and since `_sample_training_positives` only calls `rng.choice` when the window is larger than the limit, a limit at or above the ceiling does not merely give the ranker more positives, it **removes the seed's say in which positives it sees**. `RANKER_POSITIVE_LIMIT` is now an env var (same shape as `TRAIN_SEED`) defaulting to **200 000** — deliberately above the ceiling rather than equal to it, since the window moves with the dataset — and every run logs `ranker_positive_window_rows`, `n_positives_sampled` and the tag `ranker_positive_limit_binding`. Nine runs (20 000 / 60 000 / 200 000 × seeds 42/7/13, all at `COLD_START_THRESHOLD = 10` under threshold routing, which is a **new axis** and so the 20 000 arm was re-measured rather than reused): warm NDCG@10 relative range **25.36% → 8.23% → 1.68%**, cold 5.03% → 3.11% → 2.10%, and at the default all three seeds build the *identical* training set — 87,794 groups, 1,843,674 rows, to the row. A tenth run at a limit of 1 000 000 reproduces the default run on **all 36 logged metrics with zero differences**, because the window has no more to give. The same rule re-derived on the new axis (2× the largest range, rounded up, floor 0.5%) gives **`T_warm` = 6%** and **`T_cold` = 5%**, down from 58% / 7% — the warm clause goes from decorative to binding, and CF/ALS at 2.96% now sets it rather than the ranker at 1.68%. `mean_eval_result` (`src/evaluation/aggregate.py`) averages seeded runs into one `EvalResult`, refusing results that disagree about `k` or a slice's size, and `make gate CANDIDATE="<id> <id> <id>"` reads it. **The verdict: the ranker is proven better than its predecessor.** Seed-averaged against CF/ALS it is **overall +15.53%, warm +21.21%, cold +13.67% — promote**, and it promotes at each of the three seeds individually with warm +21.45% / +19.99% / +22.23%; the −4.16% / −17.24% / +13.59% warm sign confusion of the previous session was an artefact of the undersized sample. The ladder orders popularity < CF/ALS < ranker under one gate (CF/ALS vs popularity +13.14% overall, ranker vs popularity +30.71%); item-item vs popularity is **not** gateable and the gate says so with exit 2, because no popularity baseline exists at K = 500. A default run is 7.7 min against a 30-min budget. Nothing was promoted; `pipelines/` is still empty and the served bundle is untouched — `src/training/demo_artifacts.py` builds its own training set from the demo fixture and never reads this knob.
- ✅ **The demo database carries the whole MovieLens catalog, so a full-vocabulary retriever can be measured on it (`synthetic/personas/movielens-catalog.csv.gz`, `seed.py`, 2026-09-05)** — the full-data SASRec bundle published, verified and served correctly, and the k6 latency gate still could not measure it: the encoder ranks ids out of a 34,461-item vocabulary while the demo stack's own Postgres held the reviewed **120** titles, so none of its top-50 were rows, `hydrate_ranked_movies` returned nothing, and every request answered `popularity` with `fallback_reason: empty-learned-result`. That was fail-closed behaviour working exactly as designed, and it made the gate unable to see the model. `movies` has no `tenant_id` — the catalog is a property of the database, not of a tenant — so the fix is the database's, not a second tenant's: `make demo-seed` now lays all **62,423** MovieLens-25M titles under the reviewed fixture from a committed snapshot (titles, genres, `tmdbId`; 1.3 MB gzipped, regenerated by `build_movielens_catalog.py`), which is committed rather than read from `data/raw/ml-25m` because the seeder runs inside the API image — it copies `synthetic/` and never `data/` — and because a catalog that appeared only where someone had run `dvc pull` would make the reproducibility gate that trains off this database machine-dependent. Order is the decision: the reviewed rows are written **first** and every snapshot write is `ON CONFLICT DO NOTHING`, so a bulk load can never revert an editorial title (31 differ — "The Usual Suspects" for "Usual Suspects, The") or a trimmed genre string (three carry `IMAX` upstream). That second one is load-bearing beyond the product: `src/training/demo_artifacts.py` builds its feature index from an unfiltered `SELECT … FROM movies`, so a genre moving under it would move `ranker.txt`. It does not: **`make serving-artifacts-check` passes unchanged against the 62,423-title catalog** and the committed bundle was not rebuilt, because the fixture's training queries only ever name the reviewed ids and `FeatureIndex` reads `movies` only for the genre map. Measured: seeding 62,423 titles takes ≈15 s and is idempotent; materialization goes 960 → **499,384** `user_item_features` rows a generation (8 rated users × the catalog) at **81 s** wall clock for the whole `feature-setup` job; `item_features` stays at 120 because it is grouped from ratings, which is correct — an unrated item is popularity 0 and age 0 offline, and `_finite_float(None)` makes it the same online. Hydration verified through the real service: 50 ids sampled from outside the reviewed 120 hydrate 50 rows with their TMDB ids and the learned ordering intact, where before the change the same 50 would have matched nothing. One guard had to move with it: the smoke run's catalog-coverage walk is bounded at eight pages, and under its title sort it reached **1** of the 120 reviewed titles on the grown catalog and would have passed by looking at nothing — it now sorts by popularity, which is the one sort that puts exactly the seeded titles first, and reaches **120/120**. Named and not fixed: the reviewed 120 keep their posters and the other 62,303 have none, so Browse is **sparser**, not richer (enriching 62k titles is a TMDB budget with no reason behind it), the screenshot sets under `docs/frontend/evidence/` now describe a Browse that no longer exists, and `feature_store.*` appending half a million rows per release makes deployment-runbook item D8 a schedule rather than a principle.
- ⏳ **Remaining Phase 3 — product track (ADR 0012, `docs/frontend/implementation-plan.md`, `docs/frontend/finish-gate-review.md`)** — (a) the moderated sessions the finish gate is holding on, and then retiring `/legacy` in a dedicated PR against the rollback diff in `docs/frontend/README.md` (TMDB attribution no longer blocks it — PR #105 put it in the product shell); (b) `/me` subject-to-profile ownership beyond the explicit portfolio persona mode; (c) a rate-limiting decision — 7b's reliability check measured its absence (60 rapid authenticated requests, all 200, no `X-RateLimit-*`), so it needs either per-tenant limits against the tenant-config row or an ADR that records the omission deliberately — **closed by ADR 0014 and `src/serving/ratelimit.py`**, a per-`(tenant, subject)` token bucket keyed on the verified token rather than on a client address (behind an edge every request comes from a proxy), which also turns that reliability check from a recorded absence into a required gate. Two limits are deliberate and written down rather than hidden: the bucket lives in the worker process, so a four-worker service admits `workers × limit` for one subject and a Redis-backed shared bucket is the named upgrade path; and the limits are global, because the per-tenant quota column belongs with the Phase 6 tenant-config work on `public.tenants` (platform track, item (c)); (d) the two non-blocking findings the cutover left open — **N6**, the product has no persona picker now that the dashboard is off the front door (the design contract requires a labelled persona, not a picker, and the chips remain on `/legacy`), and **N7**, `button, input, select { color: inherit }` sits outside any cascade layer in `globals.css` and so out-ranks every Tailwind colour utility app-wide, which is worth fixing with `@layer base` somewhere that is not a cutover PR. **Closed by PR #96** — the three unlayered resets were verbatim duplicates of Tailwind's preflight and are deleted; what preflight does not cover is layered, and `:focus-visible` stays unlayered on purpose so `outline-none` cannot remove a focus ring.
- ⏳ **Remaining Phase 3 — platform track** — (a) ~~the ranker training path builds features with `FeatureIndex` rather than Feast's `get_historical_features`~~ — **deferred 2026-09-04, not closed**: a 2026-09-03 ADR 0009 amendment declared the boundary permanent and was withdrawn (#127) for closing the question without pricing the alternatives. The costed options are in `docs/model-planning/memos/feature-source-boundary.md`, the owner deferred the decision to prioritise modeling, and the trigger is any full-25M materialization or M2. What did land is the test rather than the decision: feature-parity CI proves all eight values agree across Python, Feast historical and Feast online at a materialization timestamp (#126); (b) ~~training-time candidate generation calls `CandidateIndex.retrieve` with no exclusions at all~~ — **closed 2026-09-04 (#126)**: `_build_ranker_training_set` computes each user's history strictly before the target timestamp and removes those titles from the negative pool, keeping the target itself as the LambdaRank positive; equal-timestamp interactions stay invisible, matching the feature pipeline's point-in-time rule. What the change does to the candidate mix is recorded as unmeasured until a full `make train-ranker` comparison exists; (c) ~~**the cold-start routing divergence the ADR 0011 cohort found**~~ — **closed 2026-08-30**: the owner took option (a) at threshold **10**, so the offline models route on ADR 0001's threshold like the deployed path does, and one number answers "is this user cold?" across the whole system (see the ✅ bullet above); both policies are being measured on `docs/offline-decisions-twotower-routing` and the choice (move the models onto the threshold, or amend ADR 0001) is mine, not an agent's; (d) **which slice the promotion gate reads** — **closed**: ADR 0001's 2026-08-30 amendment and `src/evaluation/gate.py` read overall NDCG@10 at +3% with a per-slice non-regression clause at a measured tolerance (warm 58%, cold 7%); (e) ~~**the ranker's variance, which measuring that tolerance exposed**~~ — **closed 2026-08-30**: the ranker trains on the whole trailing window, its warm spread across seeds falls 25.36% → 1.68%, the gate's tolerances tighten to warm 6% / cold 5%, and `mean_eval_result` gives Phase 4 the seed-averaged `EvalResult` to gate on (see the ✅ bullet above). Closed since the last update: per-tenant champion/quota columns (#112), generic request audits (#108), the dev/staging split (#106), the shared rate-limit bucket (#110).
